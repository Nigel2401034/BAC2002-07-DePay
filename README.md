# BAC2002-07-DePay

## Environment Setup

1. Copy `.env.example` to `.env`.
2. Fill in wallet keys, contract addresses, and API tokens in `.env`.

PowerShell:

```powershell
Copy-Item .env.example .env
```
